[package]
name = "tglock"
version = "2.0.0-beta.11"
edition = "2021"
rust-version = "1.80"
description = "Telegram unblock via local WebSocket tunnel"
license = "MIT"
autobins = false

[features]
default = ["gui"]
# The desktop GUI. Turning it off drops Tauri, the system WebView and
# frontend bundle from the build, which is what makes headless servers and
# machines without a GPU or monitor able to build and run TGLock at all.
gui = ["dep:tauri", "dep:tauri-build", "dep:open"]
# The headless binary. Deliberately NOT in `default`.
#
# When both binaries exist in one build, the Tauri bundler picks the wrong one
# as the application: `tauri build --target <triple>` packaged tglock-cli and
# shipped it as the app in v2.0.0-beta.2 and v2.0.0-beta.3. Keeping the CLI
# behind its own non-default feature means it simply does not exist during an
# application build, so there is nothing to pick incorrectly.
cli = ["dep:clap", "dep:toml"]

[lib]
name = "tglock"
path = "src/lib.rs"

[[bin]]
name = "tglock"
path = "src/main.rs"
required-features = ["gui"]

[[bin]]
name = "tglock-cli"
path = "src/bin/cli.rs"
required-features = ["cli"]

[dependencies]
tauri = { version = "2", optional = true }
open = { version = "5", optional = true }
clap = { version = "4", features = ["derive"], optional = true }
# CLI configuration file. TOML, not JSON, because the file is edited manually 
# and needs comments.
toml = { version = "0.8", optional = true }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tokio = { version = "1", features = [
    "rt-multi-thread",
    "net",
    "io-util",
    "time",
    "macros",
    "sync",
    "signal",
] }
tokio-tungstenite = { version = "0.24", features = ["native-tls"] }
# CRITICAL: "vendored" compiles OpenSSL from source, fixing the musl cross-compilation error
native-tls = { version = "0.2", features = ["vendored"] }
futures-util = "0.3"
aes = "0.8"
ctr = "0.9"
cipher = "0.4"
sha2 = "0.10"
rand = "0.8"

[build-dependencies]
tauri-build = { version = "2", optional = true }
